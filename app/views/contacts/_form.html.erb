
<%= simple_form_for ([@customer,@contact]) do |f| %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :emails %>
    <%= f.input :cellphone %>
  </div>

  <div class="form-actions">
     <%= f.submit "add/edit a contact", class: "btn btn-primary" %>
  </div>
<% end %>